'use client';

import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useEffect, useState } from 'react';
import { Copy, Download, Printer } from 'lucide-react';
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { AppShell } from '@/components/AppShell';
import { EvidenceCard, FeedbackForm, ReportClaim, ShareDialog } from '@/components/product';
import { Button, Card, ErrorState, LoadingState, StatusBadge, Toast } from '@/components/ui';
import { getReport, readableError, type Report } from '@/lib/api';

export default function ReportPage() { const { id } = useParams<{id:string}>(); const [report, setReport] = useState<Report | null>(null); const [error, setError] = useState(''); const [copied, setCopied] = useState(false); useEffect(() => { getReport(id).then(setReport).catch((err) => setError(readableError(err))); }, [id]); const exportMarkdown = () => { if (!report) return; const url = URL.createObjectURL(new Blob([report.content], { type: 'text/markdown;charset=utf-8' })); const anchor = document.createElement('a'); anchor.href = url; anchor.download = `${report.title}.md`; anchor.click(); URL.revokeObjectURL(url); }; const copy = async () => { if (!report) return; await navigator.clipboard.writeText(report.content); setCopied(true); setTimeout(() => setCopied(false), 1800); }; return <AppShell><div className="mx-auto max-w-5xl">{error ? <ErrorState description={error}/> : !report ? <LoadingState label="正在加载报告"/> : <><div className="mb-7 flex flex-wrap items-start justify-between gap-4 print:hidden"><div><Link href="/history" className="text-sm text-muted-foreground hover:text-foreground">← 返回历史</Link><div className="mt-3 flex items-center gap-3"><StatusBadge status={report.status}/><span className="text-xs text-muted-foreground">{new Date(report.created_at).toLocaleString('zh-CN')}</span></div><h1 className="mt-2 text-2xl font-bold">{report.title}</h1></div><div className="flex flex-wrap gap-2"><Button variant="secondary" size="sm" onClick={copy}><Copy className="h-3.5 w-3.5"/>复制</Button><Button variant="secondary" size="sm" onClick={exportMarkdown}><Download className="h-3.5 w-3.5"/>Markdown</Button><Button variant="secondary" size="sm" onClick={() => window.print()}><Printer className="h-3.5 w-3.5"/>PDF</Button><ShareDialog reportId={report.id}/></div></div>{report.status === 'partial' && <div className="mb-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800 dark:border-amber-900 dark:bg-amber-950 dark:text-amber-200">本报告为部分结果：可用 Evidence 不足以支持完整结论，请优先查看局限说明。</div>}<div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_280px]"><main className="min-w-0 space-y-6"><Card className="p-5 sm:p-8"><article className="report-prose prose prose-slate max-w-none dark:prose-invert"><ReactMarkdown remarkPlugins={[remarkGfm]}>{report.content}</ReactMarkdown></article></Card><section><h2 className="mb-3 text-lg font-semibold">结构化结论</h2><div className="space-y-3">{report.structured_claims.map((claim, index) => <ReportClaim key={index} claim={claim}/>)}</div></section><section><h2 className="mb-3 text-lg font-semibold">Evidence 来源</h2><div className="space-y-3">{report.evidence.map((item) => <EvidenceCard key={item.evidence_id} evidence={item}/>)}</div></section><FeedbackForm reportId={report.id}/></main><aside className="space-y-4"><Card className="p-4"><h2 className="font-semibold">数据附录</h2><dl className="mt-3 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Evidence</dt><dd>{report.evidence.length} 条</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">结构化结论</dt><dd>{report.structured_claims.length} 条</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">平台</dt><dd>B站</dd></div></dl></Card>{report.usage && <Card className="p-4 print:hidden"><h2 className="font-semibold">耗时与成本</h2><dl className="mt-3 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Token</dt><dd>{report.usage.input_tokens + report.usage.output_tokens}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">估算成本</dt><dd>${report.usage.estimated_cost.toFixed(4)}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">ASR</dt><dd>{report.usage.asr_seconds}s</dd></div></dl></Card>}<Card className="p-4 print:hidden"><h2 className="font-semibold">Agent 执行边界</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">公开分享不会暴露本区。内部轨迹用于故障定位，不作为报告事实来源。</p></Card></aside></div>{copied && <Toast message="报告已复制"/>}</>}</div></AppShell>; }
